import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sivakasipyropark.app',
  appName: 'Sivakasi Pyro Park',
  webDir: 'dist'
};

export default config;
